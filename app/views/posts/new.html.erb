<h1 class="mt-4 text-2xl font-bold text-center mb-6">連絡事項作成</h1>

    <%= render "form", post: @post %>

    <div class="text-center mt-8">
        <%= link_to t('posts.new.back_to_posts'), posts_path, class: "inline-block bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
    </div>
</div>